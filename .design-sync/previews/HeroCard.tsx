import React from 'react';
import { HeroCard } from 'rb-marine-design-system';

export const Default = () => (
  <HeroCard
    eyebrow="Marine power"
    headline="Engineered for the open water"
    body="Reliable propulsion systems trusted across the fleet, built to run in every condition."
  />
);
