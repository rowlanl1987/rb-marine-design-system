---
category: Typography
---

# Display

The largest display heading (56px) — for hero statements. Renders an `<h1>`. Defaults to white for use on dark/gradient backgrounds; pass `color` to override. `italic` for the brand's expressive tagline treatment.

```jsx
<Display>Always on.</Display>
<Display italic color="#d28dd3">always on</Display>
```
